{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/pr-codex-reminder.sh\"",
            "if": "Bash(gh pr create*)",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
